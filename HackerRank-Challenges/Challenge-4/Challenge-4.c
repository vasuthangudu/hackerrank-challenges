#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
       
    int w,d,l,total;
    scanf("%d",&w);
    scanf("%d",&d);
    scanf("%d",&l);
    
    total=w*3 + d*1 + l*0;
    printf("%d",total);
    
     return 0;
}
